# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot
# *.po # คุณอาจจะต้องการเก็บไฟล์ .po ถ้ามีการแปลภาษา

# Django stuff (ไม่จำเป็นสำหรับ FastAPI แต่ใส่ไว้เป็นมาตรฐานทั่วไป)
# *.log
# local_settings.py
# db.sqlite3
# db.sqlite3-journal

# Flask stuff (ไม่จำเป็นสำหรับ FastAPI แต่ใส่ไว้เป็นมาตรฐานทั่วไป)
# instance/
# .webassets-cache

# Scrapy stuff (ไม่จำเป็นสำหรับ FastAPI แต่ใส่ไว้เป็นมาตรฐานทั่วไป)
# .scrapy

# Sphinx documentation (ถ้าใช้)
# docs/_build/

# PyBuilder (ถ้าใช้)
# target/

# Jupyter Notebook (ถ้าใช้ในโปรเจกต์)
.ipynb_checkpoints

# IPython (ถ้าใช้)
# profile_default/
# ipython_config.py

# pyenv (ถ้าใช้)
# .python-version

# PEP 582; __pypackages__
__pypackages__/

# Celery stuff (ถ้าใช้)
# celerybeat-schedule
# celerybeat.pid

# SageMath parsed files (ถ้าใช้)
# *.sage.py

# Environments - สำคัญมาก!
.env
.env.*
!.env.example # ถ้าคุณมีไฟล์ .env.example ที่เป็น template และต้องการให้ track

# Virtual Environments
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/
# ถ้าคุณตั้งชื่อ Virtual Environment เป็นอย่างอื่น ให้เพิ่มชื่อนั้นเข้าไปด้วย
# เช่น my_project_env/

# SQLite Database files (ถ้าใช้ SQLite สำหรับ development/testing และไม่ต้องการ commit)
*.sqlite
*.sqlite3
*.db
*.db-journal # ไฟล์ชั่วคราวของ SQLite

# Alembic specific (บางครั้งอาจมีไฟล์ชั่วคราว)
# alembic.ini.bak # ถ้ามีการ backup อัตโนมัติ

# IDE / Editor specific files
.vscode/
.idea/
*.project
*.classpath
*.cproject
*.launch
*.settings
*.tmPreferences
*.sublime-project
*.sublime-workspace
*.DS_Store # macOS specific
Thumbs.db # Windows specific
desktop.ini # Windows specific

# Temporary files
*.swp
*.swo
*~
*.tmp
*.bak
*.orig

# Log files
*.log
logs/

# Operating System Files
.DS_Store
Thumbs.db
desktop.ini

# Static files that are collected or built (ถ้ามีกระบวนการ build static files)
# /static/dist/
# /static/build/

# Uploaded files (ถ้าโปรเจกต์มีการอัปโหลดไฟล์และเก็บไว้ในโปรเจกต์โดยตรง ซึ่งไม่แนะนำสำหรับ Production)
# uploads/
# media/

# Coverage data
.coverage
coverage.xml

# Mypy cache
.mypy_cache/

# Pytest cache
.pytest_cache/

# Ruff cache (ถ้าใช้ Ruff linter)
.ruff_cache/